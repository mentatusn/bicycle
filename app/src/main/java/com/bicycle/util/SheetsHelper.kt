package com.bicycle.util



/*
class SheetsHelper(private val context: Context, private val transport: HttpTransport, private val jsonFactory: JsonFactory) {
    private val spreadsheetId = "YOUR_SPREADSHEET_ID"
    private val sheets: Sheets

    init {
        val credential = GoogleCredential.fromStream(context.assets.open("credentials.json"))
            .createScoped(listOf("https://www.googleapis.com/auth/spreadsheets"))
        sheets = Sheets.Builder(transport, jsonFactory, credential).setApplicationName("Bicycle").build()
    }

    // Здесь будут функции для работы с Google Sheets
}*/
